{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "g++",
            "type": "shell",
            "command": "g++ -g -I${workspaceFolder}/12-imgui/src/vendor -lglfw -lGLEW -lGLU -lGL `find ${workspaceFolder}/12-imgui/ -name *.cpp` -o ${workspaceFolder}/12-imgui/bin/a.out && read -p \"Press Enter to continue\" < /dev/tty"
        }
    ]
}